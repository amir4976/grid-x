/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
      domains: ["localhost","amirhahmadi.liara.run","amirbucket.storage.c2.liara.space"],
    },
    output:'standalone',
  };
  
  
  export default nextConfig;